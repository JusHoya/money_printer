[Unit]
Description=Money Printer daily settlement reconcile (runs in the sandbox container)
Requires=docker.service
After=docker.service

[Service]
Type=oneshot
ExecStart=/usr/bin/docker exec mp-sandbox python scripts/settlement_reconcile.py
