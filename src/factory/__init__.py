"""Money Printer strategy factory (MaskForge-S) -- offline evolutionary search.

See PRD_STRATEGY_FACTORY.md and docs/factory/FACTORY_ARCHITECTURE.md. Only
``columns``, ``features`` and ``genome`` are numpy-only and safe for the maia
sandbox image; everything else is lab-only (pandas/pyarrow).
"""
