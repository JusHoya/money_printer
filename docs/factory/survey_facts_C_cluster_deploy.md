# C. Cluster/deployment facts (alcyone lab, maia sandbox, Hermes, result flow)

## alcyone (spark-87a1) hardware, measured 2026-09-01/02
- Ubuntu 24.04.4, kernel 6.17.0-1031-nvidia, aarch64, 20 cores, single NUMA: 10x Cortex-X925 @3900MHz = cpus [5-9,15-19]; 10x Cortex-A725 @2808MHz = cpus [0-4,10-14]. L2 25MiB, L3 24MiB. Pinned single process: 4.42 ms/cand X925 vs 8.56 A725; pool gain from pinning ~10%.
- RAM 121.7GiB unified: used 27-28GiB, free 39-41, page cache 55-56 (HF cache 107-110GB, evictable), MemAvailable ~94GiB, swap 16GiB unused, zero PSI. nvidia-smi memory N/A on GB10 -> `free -g` authoritative. torch.cuda.mem_get_info()=(38.5GB,130.7GB) tracks MemFree not MemAvailable (under-provisions ~55GiB); >90GiB allocs pressure vLLM.
- GPU GB10: CUDA 13.0, driver 580.173.02, cc 12.1, 48 SMs, 3003MHz, no MIG, idle 0%/9W. Lab image measured 84.45 TFLOPS bf16 / 20.44 fp32 (8192^2 warm). Time-shared with vLLM.
- Disk: one NVMe 3.7TB ext4 root, 220GB used, 3.3TB free, home on same fs; no NAS/NFS (pleiades storage-policy.md:37-70 assumes one). /dev/shm 61GB.
- Host clock America/Chicago (CDT); only containers pin TZ=UTC (Dockerfile.lab:7, compose.lab.yml:22); maia host Etc/UTC; Windows dev box CDT (CONTRA 9).
- Host Python 3.12.3, no numpy/pandas/torch/uv/conda. All compute must run in lab image.
- Budget (reader 4 C1): ~16/20 cores, ~80GiB peak (121.7 - ~22 vLLM - ~5 other - headroom), GPU between Hermes generations, >3TB NVMe.

## Containers on alcyone
- Docker 29.2.1, Compose v5.0.2, cgroup v2, runc; `docker info` has `cdi: nvidia.com/gpu=all`; `--gpus all` -> cuda True, plain run False.
- mp-vllm: vllm/vllm-openai:latest 20.6GB; `vllm serve ykarout/Qwen3.5-9B-NVFP4 --gpu-memory-utilization 0.18 --max-model-len 65536 --max-num-seqs 4 --enable-prefix-caching --enable-auto-tool-choice --tool-call-parser qwen3_coder --reasoning-parser qwen3`; 127.0.0.1:8000, restart unless-stopped, no cgroup limits. Fixed footprint: EngineCore 18,210MiB, RSS 4.0+3.9GB, docker stats 6.5GiB, 3.25% CPU. DO NOT TOUCH; max-model-len >=64K, Hermes max_tokens 8192, context_length 65536. Containers reach it only via --network host.
- hermes-8afd1820/hermes-08b48ba8: nikolaik/python-nodejs:python3.11-nodejs20, `sleep infinity`, 1 CPU/5GiB each, ~1MB used = Hermes terminal.backend docker (timeout 180s, lifetime 300s). Hermes gateway = host tmux `hermes`; no systemd unit, no crontab.
- Images: money-printer-lab:latest 6.3GB (633b2acc25b8, built 2026-08-31T23:37:28-0500); money-printer-sandbox:latest 555MB (e2de944a7c9f, 2026-09-01T01:15). ~/mp_stage: mp-sandbox-image.tgz 0 bytes, Kalshi_private_key_read_only.key + maia.env (600) staged on alcyone.
- Listeners: 127.0.0.1:11000 (undocumented), :8000, :631, 0.0.0.0:22, Tailscale.

## Lab image + compose
- python:3.12-slim-bookworm, Python 3.12.14, TZ=UTC, PYTHONPATH=/app, WORKDIR /app; requirements.txt minus playwright/pre-commit/keyboard + pytest httpx (Dockerfile.lab:4-22; only requirements.txt copied, code via bind mount). torch 2.13.0+cu130, numpy 2.5.2, pandas 3.0.5, scipy 1.18.1, sklearn 1.9.0, lightgbm 4.7.0, xgboost 3.4.1, optuna 4.9.0, statsmodels 0.15.0, pyarrow 25.0.1, numba 0.67.0, joblib 1.6.0, pytest 9.1.1; no polars/duckdb, no /usr/bin/time. requirements.txt unpinned (torch>=2.10, xgboost>=3.2); VM py3.14.3/pandas 3.0.1/numpy 2.4.3/torch 2.10 cpu; dev py3.11 (pandas 2.0.2 or 2.3.3 pinned). No lock file (GAP 8 partial).
- deploy/spark/docker-compose.lab.yml:15-30: `lab` binds /home/jushoya/projects/money_printer:/app rw, /home/jushoya/mp_archive/extracted:/archive:ro, lab-venv-cache vol, no ports/restart, NO gpus stanza -> `docker compose run --rm lab` CPU-only. CONTRA 0 resolved: image has CUDA torch; compose lacks device reservation.
- src/utils/logger.py:11-30,46 opens logs/money_printer_<ts>.log in CWD at import -> src.* import fails on ro /app (Errno 30); needs `--tmpfs /app/logs`. Root container litters checkout (root-owned logs/, .pytest_cache); go_no_go git_state() (:235-253) returns '' as root (dubious ownership).
- Working invocation: `docker run --rm --gpus all --user $(id -u):$(id -g) -e HOME=/tmp -e PYTHONDONTWRITEBYTECODE=1 -v /home/jushoya/projects/money_printer:/app -v /home/jushoya/mp_archive/extracted:/archive:ro --tmpfs /app/logs:mode=1777 -e TZ=UTC -e PYTHONPATH=/app -w /app money-printer-lab:latest python <script>`.
- Checkout /home/jushoya/projects/money_printer (NOT ~/money_printer; bootstrap_alcyone.sh:27 REPO_DIR=$HOME/money_printer would clone twice), revival/pleiades-2026-09 @ 24accc2 clean, 5 behind origin (f4b15f5, 5503442, 98fd8b1 ET fix, acc12be, 3c9a66b), stale remote ref. ~/.hermes/plugins/money-printer -> checkout/hermes_plugin. Tracked in checkout: data/gas_truth (aaa_daily_national.csv 304,139B; eia_weekly_regular.csv; rbob_daily.csv), calibration/, forecast_archive/, ladders/ (277 CSVs), weather_truth/, vm_snapshot_2026_08_22/reconcile_record (54). go_no_go/gas_backtest need only the checkout.
- ~/mp_archive: 6 tarballs at TOP level (market_csv.tgz 165,599,203B etc.) + archive_sha256.txt with `archive/` prefix -> `sha256sum -c` fails as written, all 6 OK after stripping; bootstrap_alcyone.sh:43-49 silently skips. extracted/ 2.1GB: 959 data_*.csv, 904 unique basenames (55 dup copies in _archive/<named run>/), 1,856,807,251B, 19,885,096 lines; _archive 855 dirs (800 cycle_*, 31 startup_*, 24 named).

## Evaluator cost on alcyone (gapfill 3 — GAP 3 RESOLVED)
- go_no_go.py --quick 11.2s/975MB, full 11.9s/976MB, single-thread (quick skips 24-cell sweep :659-681 + mixture :866-902). Reproduces reports/phase2/ws_e_go_no_go_data_2026-07-26.json with 0 differing numeric leaves; 10 provenance path-separator leaves differ (Windows `\\`) -> normalise for cross-OS hashing.
- gas_backtest.py run 641.3s, 18,990 fits (VM e2-standard-2 756.0s, 1.18x), cpu 1.00x, 235MB; ~118s/axis x5 + 26s + 27s sweep; 33.7ms/fit (gas_projection.py:751-758,816-839,858-875 interpreter-bound). CLOCK LEAK: n_accepted 198/376 -> 0 today, GAS_SCRAPE_GATE_BLOCKED 1052/1533; gas_backtest.py:1324-1331 injects clock not gate; gas_convergence.py:357,:309; aaa_provider.py:1637-1641.
- Opportunity frame 251,728 x 63 (62,932 ladder rows x2 dir x2 mode), 172.3MB, 158,286 executable, 69 dates, 1,656 markets, 276 city-days; build 1.5s cold (0.98s = 276 cached calibration refits + 0.50s frame), RSS 479MB. Harvest tape at this layout ~51M rows/~35GB.
- evaluate_shape (ev_analysis.py:1271-1335): p50 2.4ms/mean 3.8/p90 6.3/max 21.7 per candidate; 4000-draw bootstrap ~0.28ms; mask build 3.7ms; ~7.5ms end-to-end; 514/1000 random masks -> None.
- Pool on full frame saturates ~800 cand/s (1:221, 4:677, 8:733, 10fast:816, 16:799, 20:738; 744MB/worker) — bandwidth bound. Slim executable frame (158,286 x 22 cols, 16.2MB): 1.17ms single, 6,787/s pool10, 7,231/s pool20 (~15GB total). numpy kernel 0.37ms/cand/core (>20k/s box). Naive ~2.5-2.9M/h, slim ~25M/h. Zero GPU use on this path. Binding constraint statistical (69 dates), not compute; model re-price ~1.5s/candidate. Contention under active vLLM unmeasured.

## maia (Pi 4, 192.168.50.41) — gapfill 4 (GAP 4 RESOLVED; CONTRA 5 resolved)
- `ssh jushoya@192.168.50.41` key auth (from alcyone `-i ~/.ssh/id_ed25519_maia`). Repo ~/money_printer @ 3c9a66b. State /srv/money_printer/{data,logs,.env,kalshi_priv.key} on SD /dev/mmcblk0p2 (115G, 11G used, no USB disk; 6.27GB written/6 days; tape ~79MB/day; each recreate double-copies logs into shutdown_<ts>/+startup_<ts>/).
- deploy/pi/docker-compose.yml: cmd `python scripts/run_web_dashboard.py --auto-cycle --sim-balance ${SIM_BALANCE:-3000} --no-browser --host 0.0.0.0` (:23-30), env_file /srv/money_printer/.env (:38-39), `${MAIA_BIND_IP:-0.0.0.0}:8050:8050` (:45), mounts (:46-49), healthcheck /healthz 60s/10s/5/120s (:50-56), autoheal (:57-58,:71-79), restart unless-stopped (:19), mem_limit 2500m cpus 3.0 (:64-66) but HostConfig.Memory=0 (limit NOT applied). Image python:3.12-slim + requirements-runtime.txt (no torch/xgboost/lightgbm/joblib), UID 1000, tini, COPY . /app; .dockerignore excludes data/ logs/ reports/. FIVE bots active (CONTRA 6: tweets too, src/bots/__init__.py:35-38): Weather TRADING, others FEED-ONLY.
- .env keys: KALSHI_API_URL=production, KALSHI_KEY_ID, KALSHI_PRIVATE_KEY_PATH=/app/kalshi_priv.key, MP_SIM_BALANCE=3000, NWS_USER_AGENT, X_BEARER_TOKEN, X_FEED_ENABLED=1; absent MP_CONTROL_TOKEN (POST start/stop unauthenticated on LAN), DISCORD_WEBHOOK_URL, MENTION_SERIES, TWEETS_SERIES. Read-only creds, real balance $343.01 synced then overridden; no order path.
- Timers: mp-reconcile-weather 13:30 UTC (`docker exec mp-sandbox python scripts/reconcile_weather.py --days 2`; ran 09-01, 42 matched/6 NO_RESULT/0 unexplained, sim leg 0 records), mp-reconcile-settlement 06:00 UTC (not run yet), Persistent=true. No ladder capture timer anywhere (M0 kill 2026-09-15). No AAA harvester: data/gas_truth absent on maia, 59x GAS_SERIES_UNAVAILABLE.
- Tape since 2026-09-01T06:16:15Z: 9 sessions, 504,280 deduped rows (654,955 naive), gaps 5-23s, 3 auto-cycles (240min), 5 recreates, RestartCount 0. Lanes: KXAAAGASM 159,627 (66 syms), KXAAAGASW 74,739 (42), KXHIGH{NY,CHI,LAX,MIA} ~36.9k each (24 syms) + ~3.54k (F) obs rows each, KXTRUMPMENTION 43,403 (51, since 16:53Z), KXBTCY 39,312 (56), KXETHY 25,272 (36), KXPOTUSTWEETS 233 (since 00:05Z), '@realDonaldTrump (X)' 41; KXPRESMENTION/KXLEAVITTMENTION/KXELONTWEETS 0. Cadence 18.5-20.7s/symbol (2s sleep + 1s/series), X ~66s, depth 3600s (MAX_DEPTH_MARKETS gas 40/crypto 30/mention 20/tweets 20), portfolio CSV 30s (+1 row per /api/status call). Mention rows StrikeType 'custom' empty strikes -> BracketSpecError (data_loader.py:205-223). VM archive ends 08-22, maia starts 09-01: 9.3-day hole.
- Paper positions 1-3 (ML Weather NO x50: KXHIGHNY-26SEP01-T83 @0.17, KXHIGHLAX-26SEP01-B76.5 @0.37, KXHIGHMIA-26SEP01-B89.5 @0.34), marked 0.01, unrealized -42.5, exposure 44.0, expiration_time null -> can NEVER settle (matching_engine.py:1159; GAP 1 CONFIRMED). exchange_state.json saved only at cycle/close (open_position no save :1064). UTC-midnight reset double-subtracts carried book (risk_manager.py:362-375; equity 2957.5->2871.0) -> factory PnL from closed_trades/journal only. realistic_fills=False default (matching_engine.py:445). Cash re-based to 3000 each cycle/start; only cumulative_* durable.

## Result flow / promotion / Hermes
- No factory runner. Supervisor precedents: compose restart+healthcheck+autoheal (sandbox); `compose run --rm lab` one-shot; tmux; systemd timers exec into container; Hermes cron <=120s (mp_watchdog_probe.py:30); watchdog_cron.sh:37-70. No checkpoint/resume; precedents load-on-init/save-on-shutdown JSON (run_dashboard.py:257-294,300-358), dated artifacts (--as-of/--date).
- Persistence: reports/phase2|phase4 28 tracked; data/ladders 277 tracked (new days = churn); .gitignore:53-84 records cron-rewrite breaking `git pull`; data/factory/, reports/factory/ NOT ignored; lab writes straight into checkout.
- Promotion to maia only via git commit + `git pull --ff-only` + `compose up -d --build` (deploy/README.md:118-129; strategies hardcoded weather_bot.py:140-143, flags module constants) or files into /srv/money_printer/data: ModelPredictor loads data/models/*_latest.joblib at construction (predictor.py:42-43,86-126; Pi lacks joblib), MentionStrategy lazily reads data/mention_base_rates.json (reload_base_rates uncalled), lab.py logs/optimal_params.json has NO consumer. No spec loader/hot-load. Knobs: WeatherArbitrageStrategyV2(threshold=0.15, min_edge_degrees=2.0, enable_bias_correction=True) weather_strategy.py:141-149; MLWeatherStrategy(min_edge=0.08, predictor=None) ml_weather.py:72-78.
- API (src/web/server.py): GET /healthz (503 if loop >900s, side-effect free), /api/bots, /api/status (writes row), /api/portfolio_history?hours=1..2160, /api/journal, /api/training, /api/win_rates, /api/stats/rolling, /api/logs/tail|data|session, WS /ws 1Hz; POST /api/bots/{name}/start|stop (X-MP-Token). No file-serving route.
- Hermes: 13 mp_* tools (hermes_plugin/__init__.py:555-724) read MONEY_PRINTER_URL (default localhost:8050) / MONEY_PRINTER_DIR (default ~/money_printer, absent on alcyone); local-file-first fallbacks (:142-148,:400-419) would misreport lab state; register(ctx) :732-741. Cron: mp-watchdog 30m (no-agent ~/.hermes/scripts/mp_watch.sh, healthy); mp-status 240m FAILING drift_skip since 35B->9B swap — fix `hermes cron edit fdc309363b09 --provider custom --model ykarout/Qwen3.5-9B-NVFP4`; pin --model/--provider on all new jobs. Shapes: --script, --no-agent (empty stdout silent), --monitor-script/--monitor-url (byte-hash, timestamp-free output), --continuity; deliver discord:1491982736989093961; `hermes send`. Webhook helper src/notifications/discord.py:160-171 (VM webhook exposed, rotate). Plugin symlinked -> new tool = git pull + Hermes restart.
- F1: no non-loopback binds on alcyone (trust-boundaries.md:48-85; deploy/README.md:49-53); ~/.hermes/ syncs to maia — no secrets there; keep .env out of alcyone checkout (visible rw in lab).
- Replay needs no Kalshi creds (ladders manifest auth_mode anonymous; kalshi_history.py:286-293). Retention now ~67-73 days; 07-26..08-31 ladders expire ~2026-10-03.

## Critic status (in scope)
- GAP 3 compute: RESOLVED (gapfill 3). GAP 4 maia: RESOLVED (gapfill 4). GAP 8 reproducibility: PARTIAL (lab==VM to the digit; no lock; Windows diff + pytest not run). GAP 1 expiration_time: CONFIRMED, fix owed. CONTRA 0/5/6/9 resolved as noted.
- Open: fix bootstrap_alcyone.sh path+archive layout; add `gpus: all`/--user/logs tmpfs to compose lab; logger import side effect (MP_LOG_DIR); fast-forward alcyone; supervisor (2nd compose service vs tmux); ignored data/factory/<run_id>/gen_<N>/ + reports/factory/; lock + env hash per generation; spec loader; mp_factory_* tool vs monitor cron; ladder capture; USB-SSD; MP_CONTROL_TOKEN; unapplied mem limit; port 11000; vLLM for mutation (host netns, 4 seqs, Hermes contention).
